<% if User.current.allowed_to?(:edit_catalogs, nil, :global => true) %>
  <h1><%= t('organization.action.new.title') %> </h1>

  <%= render partial: 'form', locals: { organization: @organization, action: :create } %>
<% else %>
  <p class="nodata"><%=t('permission.denied.message')%></p>
<% end %>
<%= link_to t('button_cancel'), {:controller => 'organizations', :action => 'index'} %>
